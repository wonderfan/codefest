如何响应采购的要求？技术规范、参数和要求，投标报价表、投标产品技术条款影响表，技术文件和资料，售后服务承诺书，要求和内容梳理清楚，大数据存储、处理和展示系统，将题眼进行剖析，在纵向的多个功能的维度之后，横向的一条的维度，安全和监控的方面的要求。安全的需求的具体的分类：用户安全、数据安全、平台安全。

功能级别的权限和数据级别的权限管理，大数据在航空系统的研究现状与发展趋势；虚拟机、虚拟化程序、硬件和处理器的操作接口、主机的操作系统、多核处理资源、系统总线、内存控制器、输入和输出控制器、处理器内部资源和处理器外部资源、主存储器、输入和输出设备、网络、临时隔离与空间隔离、干涉的分析临时干涉、时间的延迟性。

容器技术：分区的技术切换根目录，底层的基础设施分开，系统容器、容器包含了组件，包含文件、环境变量、依赖和库文件。主机的操作系统对物理资源进行访问限制。物理资源包括处理器、存储和内存。镜像文件是应用和服务的静态状态和表示。在静态不变的层之上有一个可写和可读的层级。清单、文件系统层级和配置文件

处理器的几种不同的模式：用户模式、中断处理、监管者模式、操作系统的保护模式、指令没有定义的错误中、特权用户的模式、监控的模式、指令处理的条件化、条件性指令的执行、分支指令、对指令进行分支和条件性的处理、数据处理指令、内存区域的访问的权限、内存的控制逻辑和内存的管理单元，数据需要从内存加载到处理器中去处理。指令的提取、指令的解码、指令的执行和加载与储藏。

处理器的架构和计算机的架构以及操作系统的架构、冯诺伊曼架构、架构不同的画像、应用画像（内存管理、高性能、多任务、多用户的操作系统、信任区）、实时的画像：内存的保护、低延迟和预测性、微控制器画像。

使用流水线加快指令到处理的处理的过程，内存系统、内存层级、内存顺序模型

设计和行为的分析：指令、信息单元、寄存器、输出和输入控制、执行模型.

内核系统的模块的开发者，对文件系统的各个方面都有涉猎。文件的创建的过程与改进的余地。使用挂载的机制来实现文件的创建活动。文件的权限在不同的用户之间的访问控制的问题。权限的执行.

