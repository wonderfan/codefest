构造具体的结构体类型实现历史记录的查询和执行；实现的功能列表有：根据namespace和key查询出此key的变动历史；history的是否开启取决于配置，如果开启的话，具有历史查询的功能；构造一个key的范围，从数据库中查询此范围对应的迭代器和游标集。在这里面隐含着一个内在关系的重大理解，那就是状态数据库中的数据和block文件中的数据的对应映射关系。如何有效，准确的理解这段关于byte数组的操作？如何有趣的阐述基于key的range query？这是涉及到了key value数据库的机制和原理了吗？account的实质和内涵分别是什么？key pair和signature的加密算法是什么？把当前的结构体对象进行json序列化之后，按照提供的文件名和路径进行本地的文件存储；简单来说就是写入到文件。基于sdk的account体系设计和存储实现；根据对象的实例经过反射之后获得对象的类型；基于对象的类型，进行对象类型的构造分析；分析的方面包括有多少个feild以及多少个methods；understand the accout and wallet concept as well as their relationship.
